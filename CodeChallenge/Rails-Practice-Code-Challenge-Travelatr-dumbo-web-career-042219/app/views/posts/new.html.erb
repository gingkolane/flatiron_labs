<%= form_for @post do |f| %>
Blogger: <%= f.collection_select :blogger_id, Blogger.all, :id, :name %>
Destination: <%= f.collection_select :destination_id, Destination.all, :id, :name %>
Title: <%= f.text_field :title %>
Content: <%= f.text_field :content %>
<%= f.submit  %>

<% end %>