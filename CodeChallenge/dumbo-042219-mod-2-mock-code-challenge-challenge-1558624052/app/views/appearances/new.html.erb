<h1>Create a new appearance</h1>

<%= form_for @appearance do |f| %>
  <%= f.label :rating, "Episode rating"%>
  <%= f.number_field :rating %>
  <br>

  <%= f.label :guest_id %>
  <%= f.collection_select :guest_id, Guest.all, :id, :name%>
  <br>
  <%= f.label :episode_id %>
  <%= f.collection_select :episode_id, Episode.all, :id, :date %>
  <br>
  <%= f.submit %>
<% end %>

